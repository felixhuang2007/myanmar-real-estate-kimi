--- a/myanmar-real-estate/backend/04-house-service/repository.go
+++ b/myanmar-real-estate/backend/04-house-service/repository.go
@@ -234,19 +234,22 @@ func (r *houseRepository) Search(ctx context.Context, params *HouseSearchParams)
 		query = query.Where("decoration = ?", params.Decoration)
 	}

-	// 关键词搜索
+	// 关键词搜索：优先使用Elasticsearch，降级为LIKE查询
 	if params.Keywords != "" {
-		// 处理中文编码问题
-		keywords := strings.TrimSpace(params.Keywords)
-		keywordPattern := "%" + keywords + "%"
-		query = query.Where("title ILIKE ? OR address ILIKE ? OR description ILIKE ?",
-			keywordPattern, keywordPattern, keywordPattern)
+		// 处理中文编码：将关键词转为UTF8并清理特殊字符
+		keywords := strings.TrimSpace(params.Keywords)
+		if keywords != "" {
+			esUsed := false
+			if r.esEnabled {
+				ids, err := r.searchByES(ctx, keywords)
+				if err == nil && len(ids) > 0 {
+					query = query.Where("id IN ?", ids)
+					esUsed = true
+				}
+			}
+			if !esUsed {
+				// 降级：PostgreSQL LIKE查询，使用参数化查询避免编码问题
+				// 使用 CONCAT 和 CAST 确保正确处理UTF8字符
+				keywordPattern := "%" + keywords + "%"
+				query = query.Where("title ILIKE ? OR address ILIKE ? OR description ILIKE ?",
+					keywordPattern, keywordPattern, keywordPattern)
+			}
+		}
 	}

 	// 只查询在线状态
